vortex app stack installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Services (app layer only — this umbrella does NOT manage data/platform infra):
  - gateway        REST entry            ClusterIP :3000
  - orders         REST + NATS producer  owns Postgres
  - inventory      REST sync query       owns Postgres
  - notifications  NATS consumer         (no Service)
  - analytics      NATS consumer         (no Service)

Before pods become Ready, ensure these exist in namespace "{{ .Release.Namespace }}":
  1. Data + platform bootstrap (applied separately, see deploy/k8s/{data,platform}):
       Postgres + NATS, and the Gateway API resources for external entry.
  2. Per-service Secrets referenced by the charts:
       kubectl get secret orders-secret inventory-secret -n {{ .Release.Namespace }}
     (create from gitignored manifests now; Sealed Secrets at the GitOps phase.)

Check rollout:
  kubectl get pods -n {{ .Release.Namespace }}
  helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
