apiVersion: v1
kind: ConfigMap
metadata:
  name: orders-config
  namespace: vortex
data:
  PORT: "3000"
  INVENTORY_URL: "http://inventory:3000"
  NATS_URL: "nats://nats:4222" # in-cluster broker DNS (headless Service); orders publishes order.created here
