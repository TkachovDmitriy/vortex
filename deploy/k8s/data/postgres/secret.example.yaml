# Template — copy to postgres-secret.yaml and set real values (that file is gitignored).
# Superuser credentials for the Postgres StatefulSet.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: vortex
type: Opaque
stringData:
  POSTGRES_USER: CHANGEME
  POSTGRES_PASSWORD: CHANGEME
