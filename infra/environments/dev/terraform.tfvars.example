# Copy to terraform.tfvars (gitignored) and fill in. Never commit the real file.

# Your public IP as a /32. Find it with: curl -s https://checkip.amazonaws.com
allowed_cidr = "203.0.113.4/32"

# Optional overrides:
# region          = "eu-central-1"
# instance_type   = "t3.medium"          # 4 GB for full stack incl. metrics
# public_key_path = "~/.ssh/id_ed25519.pub"
